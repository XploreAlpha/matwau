# MatWAU 依赖(Stage 1 mock)
# per MatWAU-开发计划 §5.6 W8 测试 + 优化
# 本机无 GPU / 超算,只用纯 Python + mock

# 测试
pytest>=7.0
pytest-cov>=4.0
pyyaml>=6.0

# 数据
pydantic>=2.0

# W32 — LineageStore PostgresBackend 依赖(可选;无 psycopg 自动降级到 SQLite)
psycopg[binary]>=3.1

# W33 — LLMReviewer(可选;无 openai 包 → 自动跳过 LLM 复核)
openai>=1.0

# W37.12 — wau_protocol_adapter 注册到 wau-registry + JWT 签发
# requests 走 wau-registry HTTP API, PyJWT 签 HS256 跟 wau-edge 验签
# (实际运行环境已装 requests 2.32.5 + PyJWT 2.10.1;写明便于学院方 pip install)
requests>=2.31.0
PyJWT>=2.8.0,<3.0

# v1.4.1-Academic — paper PDF parse only(本地上传 + 语义搜索已删)
# pdfplumber: pure Python / MIT / ~5MB(放弃 PyMuPDF GPL)
# 注:v1.3.4-Academic 还用 scikit-learn 做本地 TF-IDF,语义搜索 pipeline 整体删除后无依赖
pdfplumber>=0.10.0

# Lint(可选,CI 用)
ruff>=0.1.0